---
apiVersion: apps/v1
kind: Deployment
metadata: 
  name: "mindscopedep"
spec: 
  replicas: 3
  selector:
    matchLabels:
      dc: "In"
  template:
    metadata:
      name: "mypod"
      labels:
        dc: "In"
    spec:
      containers:
      - name: "frontend"
        image: ashishdev213/wellness-frontend
        imagePullPolicy: Always
        ports:
        - containerPort: 5000 
      - name: "backend"
        image: ashishdev213/wellness-backend
        imagePullPolicy: Always
        ports:
        - containerPort: 8000 

---

apiVersion: v1
kind: Service
metadata:
  name: "mindscopeservice"
spec:
  selector:
    dc: "In"
  ports:
  - port: 5000
    targetPort: 80
    nodePort: 31933
  type: LoadBalancer